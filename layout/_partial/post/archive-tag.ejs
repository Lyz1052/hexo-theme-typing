<% if (post.tags && post.tags.length){ %>
  <% var tagHtml='<div class="archive-article-tag">'; %>
  <% post.tags.each(function(tag, i){ %>
      <% tagHtml+="<a href='"+ url_for(tag.path) +"'>"+tag.name+"</a>"%>
    <% }) %>
  <% tagHtml+='</div>'%>
  <%-tagHtml%>
<% } %>
